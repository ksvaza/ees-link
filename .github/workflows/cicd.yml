name: CI Pipeline
on: [push]
jobs:
  build:
    steps:
      - uses: actions/checkout@v6
      - name: Setup JDK 14
        uses: actions/setup-java@v4
        with:
          java-version: '14'
          architecture: x64
